matilde sem posts

write post

FAVORITES DESTINATION
falta cloud

fazer um documento data rules
modelaar os dados

terminar o processo para obter dados pesquisados (por um progressbar onde aparece butao)

fazer com que seja possivel conversar e escrever posts





MYPROFILEFRAGMENT && PROFILEFRAGMENT
merge into one class -> {
    simple check if the nullable id is passed or not
}

USERPOSTSFRAGMENT && POSTSFRAGMENT
merge into one class -> {
    require two nullable strings (topicId and userId)
    sequent actions are based on which is not null
}

READFRAGMENT && PROFILEFRAGMENT
no need for dealing with lists (
    simple check if id isEmpty
    only if not stop showing progressbar
)